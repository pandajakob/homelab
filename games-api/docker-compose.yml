services:
  games-api:
    image: jakobjakob/games-api:1.0
    container_name: games-api
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - web
volumes:
  portainer_data:
networks:
  web:
    external: true
